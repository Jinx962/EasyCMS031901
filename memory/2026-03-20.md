# 记忆 2026-03-20

## 变更类型
里程碑 + 基础配置

## 变更摘要
完成阶段一前端部分开发（后台管理模块），并对项目基础配置进行多项补充：新增 `memory/`、`technology/` 目录及规则，重构 `progress/` 为并行进度体系。

## 变更详情

### 1. 前端工程初始化（app/frontend/）
- 技术栈：Vite + React 18 + TypeScript + TailwindCSS 4 + shadcn/ui + React Router 7
- 目录结构：`app/frontend/src/app/`（组件与页面）+ `app/frontend/src/styles/`
- 复制全部 47 个 shadcn/ui 组件到 `app/frontend/src/app/components/ui/`

### 2. 修复的问题清单

| 文件 | 修复内容 |
|------|---------|
| AdminLayout | 顶部导航动态 active；面包屑去重；其他模块标记暂未开放 |
| UserManagement | 移除无用 Filter 导入；受控筛选；Checkbox 批量选择+确认弹窗；新增「锁定」状态；表单状态管理；功能性分页 |
| RoleManagement | 新建角色弹窗+表单；复制角色确认弹窗；停用角色确认弹窗；停用状态红色 badge；受控筛选；功能性分页 |
| UserDetail | 根据 URL id 参数查找用户；停用确认弹窗；编辑弹窗+保存逻辑 |
| PermissionConfig | 左树 checkbox 联动右侧矩阵；模块半选态；操作权限依赖 view 权限；新增「复制自其他角色」按钮 |
| AuditLog | 修复分页按钮中文乱码；受控筛选；功能性分页 |
| NotificationConfig | Switch 开关真正控制启用状态并有 toast；受控筛选；功能性分页 |
| sonner.tsx | 移除 next-themes 依赖，改为固定 light 主题 |

### 3. 构建验证
- TypeScript 类型检查：0 错误
- Vite 生产构建：成功（513KB JS + 91KB CSS）

### 4. 目录结构调整
- 前端代码从 `app/` 根目录移动到 `app/frontend/`，为后续 `app/backend/` 预留空间

### 5. memory/ 目录补充
- 创建 `memory/README.md`（触发条件与格式规范）
- 补写历史记忆文件 `memory/2026-03-18.md`
- 在 `AI_PRD_GUIDE.md` 和 `rule.md` 中新增「memory 记录规则（规则 7）」

### 6. technology/ 目录新增
- 创建 `technology/README.md`（使用说明与规则）
- 创建 `technology/TECH_STACK.md`（前端技术选型：Vite + React 18 + TS + TailwindCSS 4 + shadcn/ui；后端待定）
- 在 `rule.md` 和 `AI_PRD_GUIDE.md` 中新增**规则 0.1：开发前必读技术选型**，以及 `technology/` 目录标准模板（2.12 节）

### 7. progress/ 并行进度体系重构
- 问题：原有 `CURRENT_PROGRESS.md` 为单一文件，前后端/UI/测试并行开发时写同一文件会产生冲突
- 解决方案：拆分为「主控总览 + 各端子进度文件」
  - `CURRENT_PROGRESS.md` → 重写为**主控总览**（当前阶段 + 各端状态速览表 + 主要阻塞项）
  - 新增 `progress/frontend.md`（前端子进度，含阶段一已完成/待完成明细）
  - 新增 `progress/backend.md`（后端子进度）
  - 新增 `progress/ui.md`（UI 设计子进度）
  - 新增 `progress/tests.md`（测试子进度）
  - `PHASES.md` 更新说明，指向各端子进度文件
- 规则更新：`rule.md` 规则 2 新增「2.2 并行开发时的进度文件规范」，明确各端只写各自子文件；规则 1.3 更新，未完成项写入各端子进度文件
- `AI_PRD_GUIDE.md` 更新：目录清单补充子进度文件；2.6/2.7 模板更新；新增 2.7.1~2.7.4 各端子进度模板

## 影响范围
- `progress/`（结构性重构：新增 4 个子进度文件；CURRENT_PROGRESS.md 重写为主控总览）
- `rule.md`（新增规则 0.1、规则 2.2、规则 7；规则 1.3 更新）
- `AI_PRD_GUIDE.md`（目录清单、2.6、2.7 模板更新；新增 2.7.1~2.7.4、2.9、2.12 节）
- `app/frontend/`（全新前端工程）
- `memory/`（新增目录）
- `technology/`（新增目录）

## 当前状态
- **当前阶段**：阶段一（进行中）
- **前端已完成**：后台管理全部 9 个页面（AdminLayout、UserManagement、UserDetail、RoleManagement、RoleDetail、PermissionConfig、AuditLog、NotificationConfig、SystemParams）
- **前端待完成**：登录/登出页面、路由守卫（阶段一要求）
- **后端待完成**：数据库 Schema、JWT 鉴权 API（阶段一要求）
- **已知问题**：当前数据为前端 mock，尚未对接后端 API
